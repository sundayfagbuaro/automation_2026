apiVersion: v1
kind: Service

metadata:
  name: ggpa-service
  namespace: ggpa-system

spec:
  type: NodePort

  selector:
    app: ggpa

  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30007